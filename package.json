{
  "name": "graphis",
  "version": "1.0.0",
  "private": true,
  "description": "Decentralized academic citation & knowledge graph engine (static frontend + Vercel serverless functions)",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
